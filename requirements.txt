PySide6>=6.5
requests>=2.28
psutil>=5.9
pywin32>=306; platform_system == "Windows"
edge-tts>=7.0
zstandard>=0.22

# Optional: NVIDIA GPU temperature monitoring (GPU alerts are skipped when absent)
# pynvml
